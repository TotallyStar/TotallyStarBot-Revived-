# Put the real token in your host's secret store, not in Git.
DISCORD_BOT_TOKEN=replace-with-your-bot-token

# Optional: register /say only in a test server for faster updates.
# DISCORD_GUILD_ID=your-server-id
